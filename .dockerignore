# Never let secrets or recordings into an image layer.
**/.env
**/.env.*
!**/.env.example
apps/server/eval-clips
apps/server/tmp

# Reinstalled inside the image; copying them in is slow and can mix architectures.
**/node_modules
**/dist
.git
.expo
