services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "8787:8787"
    env_file:
      - apps/server/.env
    environment:
      - PORT=8787
    restart: unless-stopped
